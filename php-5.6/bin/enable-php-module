#!/bin/sh
set -e

if [ -z "$XDEBUG_CONFIG" ]; then
    phpdismod xdebug
fi
if [ -z "$BLACKFIRE_SOCKET" ]; then
    phpdismod blackfire
fi

# first arg is `-f` or `--some-option`
if [ "${1#-}" != "$1" ]; then
    set -- php "$@"
fi

exec "$@"
